# Backup Snapshot: 2026-07-19

This snapshot folder records the pre-GitHub-backup packaging state of the LASSO project. It is intentionally lightweight: the project itself is preserved in place, and the Git commit created from this state is the durable backup mechanism.

Pre-backup observations:

- The folder was not a Git repository before the packaging pass.
- Important research materials existed under `data/`, `code/`, `results/`, `papers/`, `docs/`, and `archive/`.
- Disposable local/build folders existed: `.venv/`, `tmp_biber/`, and `Stata 17/`.
- Raw Enterprise Survey data and Word drafts should be treated as sensitive and backed up only to a private repository unless public-release permission is explicit.

